char-rnn-tensorflow server
==========================

Async server with dynamic model reload
--------------------------------------

Asynchronous server uses `aiohttp` and works only on Python 3.5   

### Install

```sh
pip3 install requirements-async.txt
```

